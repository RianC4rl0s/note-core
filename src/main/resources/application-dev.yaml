spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/note-core
    username: postgres
    password: teste

  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: true
